# BigQuery — only datastore, nothing else
BIGQUERY_PROJECT=mis-gempundit
BIGQUERY_DATASET=Content_FMS
GOOGLE_APPLICATION_CREDENTIALS_JSON=   # paste service-account JSON on one line

# Dev: who to act as when no real auth (approver | writer | admin)
DEFAULT_ACTOR_EMAIL=vivek@gempundit.com

# Machine auth for /api/keywords /api/drafts /api/sync
WORKFLOW_API_KEY=

# Google Workspace SSO (future)
# REQUIRE_SIGNIN=0
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_WORKSPACE_DOMAIN=gempundit.com
